# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

For the hero product, keep the SNAPOD booth stationary and use a five-color segmented SVG scan that follows the product silhouette with a very small uniform gap. Keep color-transition gaps short, place subtle ticks inside the colored path, and do not reintroduce a circular orbit around the product.

The 2026-08-19 homepage brief supersedes the previous silhouette-scan direction for this prototype. Use one large circular five-segment halo around the stationary product, with about one percent circumference gaps, independently counter-rotating inner ticks, dark concentric tracks, and one restrained lower-right highlight arc. The booth itself must remain stationary and must never inherit the halo rotation.

The 2026-08-19 browser annotation refines that halo into a perspective product orbit: keep the same five segments and independent rotation, but tilt and depth-shade the orbit so it reads as surrounding the booth rather than as a flat screen-space circle. A CSS perspective treatment is acceptable for the lightweight version, but it must not be described as true Three.js depth occlusion.

The intended treatment is specifically a two-dimensional compositing illusion of three-dimensional depth. Split the halo into a back half below the transparent WebGL canvas and a front half above it, so the booth occludes the far arc while the near lower arc passes in front. Reinforce the effect with a thinner, dimmer far stroke and a thicker, brighter near stroke; do not turn the halo into actual Three.js geometry.

The user's selected visual direction on 2026-08-19 supersedes both halo instructions above. Implement the third concept, "Quiet Light Volume": remove the rainbow ring entirely, make the local SPD01 GLB the sole hero-product geometry, reveal the real black frame, clear glass, gray felt, sage side panels, light desk, stool, and ceiling light with a restrained cool-white top light, show thin quieting sound lines only inside the verified front-glass mask, keep broader muted-red wave traces outside the booth, and retain only a low elliptical floor reflection/contact shadow. Do not copy the concept image's invented booth geometry, and do not imply measured acoustic performance where no verified data is available.

The later 2026-08-19 refinement keeps the third concept's local-model placement but replaces its sound-field motion language with the first concept: four larger muted-red incoming wave bands should slow and settle at the front glass, five thin sage/cool-white waves should continue inside the verified glass mask and emerge quietly on the far side, and one restrained diagonal reflection should sweep across the glass. The first concept is a motion reference only; never copy its invented booth proportions, framing, or geometry over the authoritative local GLB.

The 2026-08-19 product-fidelity correction uses the supplied `spd01-green-transparent.webp` as the exact hero appearance layer because the prior CAD-only render visibly drifted from the local product photography. Keep the fixed WebGL canvas alive behind it, then crossfade into the optimized, semantic STEP-derived `snapod-spd01-authoritative.glb` for the structure and following chapters. Do not procedurally invent a replacement stool, desk, felt wall, or light; the local raster and STEP-derived assembly are the only product-shape sources.

Match the Anime.js homepage's motion hierarchy rather than copying its Canvas renderer: the product-edge color scan should complete a loop in about 72 seconds, the inner ticks should move more slowly in the opposite direction, and the front-glass visualization should use horizontal red sound-field lines with a dotted S curve. Quiet mode reduces amplitude and opacity; it must not move or transform the booth.

The 2026-08-19 glass-dot correction supersedes the dotted-curve playback above: keep five restrained dots as one static glass marker cluster. Do not animate them along a motion path or restart them on quiet toggles, scroll, resize, visibility changes, or hash refresh. Anime.js spring motion remains reserved for the real draggable interaction control.

Do not reintroduce the `SPD01 / SAGE` caption beneath the hero product; the user intentionally removed this redundant label on 2026-08-19.

The strengthened sound-field treatment approved on 2026-08-19 uses higher-amplitude muted-red incoming waves, clearer sage/cool-white glass and outgoing lines, and a slightly brighter restrained haze. Keep the desktop contrast stronger while preserving the existing compact/mobile attenuation, quiet-mode collapse, and static glass-dot rule.

The 2026-08-21 `100cqh` motion request is an entrance treatment, not a product exit or floating loop. When the page first opens at the hero, move the whole product stage from `y: 100cqh` to `0cqh` over 1250ms with Anime.js `out(1)`, then bring the complete sound field in with a restrained 110ms stagger. Play it once only, skip it for reduced motion and direct non-hero anchor refreshes, and never restart it on quiet toggles, scrolling, resize, visibility changes, or hash navigation. After settling, the booth must remain stationary.

Chapter changes must begin while the following section enters the final 68% of the viewport and finish at its top boundary. Drive the hero raster/GLB blend, stage center, active chapter semantics, and global rail-label emphasis from the same scroll-derived transition weight so fast scroll, reverse scroll, and anchor refresh do not expose a hard cut.

The hero-raster to structure-GLB handoff must not use a direct linear crossfade because the photographic product and semantic GLB have different silhouettes and produce a duplicated booth at mid-blend. Use the approved scroll-linked dip-to-dark handoff: hold the raster first, fade it out by roughly the midpoint, begin the GLB just before the midpoint, and bring the GLB fully in during the following quarter. Keep overlap minimal and opacity response short so fast or reverse scrolling cannot leave a ghost layer.

The modular card redesign uses one compact reference-style segmented composition bar plus one horizontal row of five labeled keys. Each of the five real module groups owns a stable, distinct color (frame red, exterior orange, glass cyan, acoustic lime, furniture violet); the labels and reference indices must remain visible so color is never the only identifier. Keep the card in the lower-left on desktop and do not restore the former tall panel or five identical orange line bars.

The sound-volume redesign uses amplitude as the primary visual encoding: broad, low-opacity red energy envelopes sit behind the four incoming core waves, while the transmitted and glass waves remain thin. A qualitative five-segment `外側音量 / 大` meter and two-segment `透過音量 / 小` meter make the contrast explicit without showing unverified dB values. Meter segments reveal once, never loop, and the five glass dots remain static.

The 2026-08-22 acoustic-graphic refinement keeps those established counts: four large incident red bands and five thin glass/transmitted sage lanes. Give every lane a faint technical baseline, taper the glass amplitude along its path, taper the transmitted amplitude and opacity farther from the booth, and use restrained directional gradients instead of stronger bloom. The three regions must read as one attenuation story without claiming numeric dB performance or adding a continuous wave loop.

The acoustic waves must not run an infinite path-`d` morph loop. Reveal the base wave field once, then animate each full SVG lane group with deterministic alternating `scaleY` so the complete line breathes: strong incoming amplitude, smaller glass amplitude, and smallest transmitted amplitude. Keep the one short normalized `stroke-dashoffset` overlay per path for direction. Keep all five glass dots static, pause every loop with page visibility, and omit loops for reduced motion. Use the acoustic section's scroll progress to reduce the underlying glass waveform and move the reflection. The volume meters already identify the two sides, so do not add a second bottom wave legend. On desktop, keep the incoming field to the right of the chapter copy rather than drawing through the paragraph.

Do not share one desktop sound-field channel layout between hero and acoustic states. Both states may use the same right-biased product center, but the hero uses narrower channels around the photographic silhouette while the acoustic chapter uses channels aligned to the GLB's outer left and right edges. Incoming red waves must stop outside the visible shell, transmitted sage waves must begin beyond the right panel, and both must clear the copy. Mobile keeps its dedicated centered layout.

The structure chapter must reserve a compact upper-left copy exclusion zone. On desktop, arrange part labels as two aligned technical callout stacks: a right-side upper/middle stack and a left-side stack that begins below the copy. Long hairline leaders may cross open background but no label may enter the copy box or the far-right progress-rail gutter. Keep each callout mapped to its real deterministic exploded part; this layout rule must not change the model geometry or explosion directions.

The structure explosion belongs only to the structure chapter. It must expand, hold briefly, and fully reassemble before the acoustic chapter becomes active; never carry a partial explosion into the glass/sound chapter, because that stacks two chapter effects and makes the acoustic comparison unreadable. Reverse scrolling must retrace the same deterministic reassembly curve.

The structure chapter needs enough scroll distance to explain the assembly. On desktop use a roughly 250lvh section with an explosion rhythm near 38% separating, 40% fully exploded and readable, and 22% reassembling. Mobile may shorten this to roughly 190lvh. Reduced-motion users should not be forced through the extended stage. Keep the full hold state long enough for every callout to be read, while still reaching zero explosion before acoustic becomes active.

Structure callout leaders must terminate on live screen projections of their corresponding semantic GLB parts and update through explosion, reassembly, camera changes, and resize. Mark every target with a restrained endpoint ring and dot. Labels must describe nodes that actually exist in the authoritative local GLB: use `固定ガラス` and `床カーペット`; do not restore the unsupported `背面ガラス` or `スツール` labels unless the source model is replaced with verified geometry containing those parts.

Keep every structure callout label, its label-side horizontal origin, and its elbow fixed in screen space. During model motion, update only the projected target ring/dot and the final connected leader segment. Never apply `createDrawable()` to these live-mutated leader paths because its cached dash length can split the line after the path changes; reserve the SVG draw treatment for the fixed-circumference endpoint rings.

Use one fixed, continuous whole-page scroll progress rail. Its cursor must map to `scrollY / (scrollHeight - innerHeight)` and move continuously with transforms; do not show separate module progress meters or advance the cursor in discrete feature-sized jumps.

On desktop, reserve the far-right edge for that one global progress rail and arrange it vertically so it cannot pass under copy, product graphics, or floating panels. On viewports at or below 900px, switch the same rail back to a compact horizontal bottom layout. Module and interaction panels must reserve the rail gutter and must not overlap the booth; verify this at short desktop viewports as well as mobile.

Size the desktop hero product to roughly 65% of the viewport height, matching the reference homepage's dominant visual height while preserving the booth's narrow aspect ratio. On mobile, keep it near 35% of viewport height so the title and controls remain unobstructed; the status label must sit below the enlarged product.

The authoritative hero assembly is `Snapod(小号)静音仓-W1000D1000H2300-总装 - 门樘-固定桌板-常规线路-可拆侧板.stp` from the supplied `002 SNAPOD 左开门（可拆钢板+加门樘+底部通风）` folder. The STEP has usable geometry but no reliable face colors, so GLB conversion must infer the sage exterior panels, gray felt interior, transparent front/rear glass, light desk, dark floor, ceiling, and charcoal frame from part geometry. Do not fall back to a uniformly dark material model.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

The 2026-08-22 design-system pass targets Japanese enterprise facilities buyers and interior designers. Keep the page on a quiet-industrial, Japanese-negative-space axis: an N9 edge-aligned header with only the SNAPOD mark and one `導入相談` action, a full-bleed product-led hero, solid low-radius instrument panels instead of decorative glassmorphism, and an Ft2 single-line desktop close. The existing global progress rail remains the only chapter navigation. Treat Material 3 Expressive as an interaction-quality reference only (48px touch targets, state clarity, reduced motion), not as a new visual skin. Reuse `tokens.css` and update `DESIGN_SYSTEM.md` whenever this visual system changes.

The 2026-08-24 Japanese-UX refinement removes decorative English register labels and unverified pseudo-metrics from the instrument panels. The module bar is a five-part qualitative classification diagram with equal visual weight, numbered Japanese labels, and an explicit `数量比ではありません` disclosure; do not restore arbitrary 88/72/64-style values. Keep the spring panel title and instructions in Japanese, and keep all five chapter labels visible on the same bottom progress rail at phone widths so navigation is not reduced to unlabeled ticks.

On tablet and phone widths, the modular and interaction chapters use a two-channel composition rather than a full-width bottom sheet. Both compact cards stay at lower-left and translate only the WebGL product toward the right. The chapter copy remains fixed at the top and both panels stay above the global progress rail. Do not solve overlap by making a panel transparent or hiding the product.

The interaction card shares the modular card's lower-left anchor on desktop as well. Keep the real 48px draggable target, spring return, qualitative labels, and live status text intact; compact spacing rather than hiding functionality. Reserve clear gaps above the footer, below the chapter CTA, and before the product silhouette. After even an extremely fast release, clear the returning state once the draggable's live x value reaches center; do not leave stale spring-status text onscreen.

The 2026-08-24 hero sound-field boundary correction makes the authoritative transparent SPD01 WebP the coordinate source for the acoustic overlay. Derive the visible left shell, right shell, and front-glass opening from its live rendered rectangle so resize and responsive layout keep the lines attached to the product: incident red waves stop 7–10px before the left frame, glass waves stay inside the opening, and transmitted sage waves begin 7–10px beyond the right panel. Reveal these regions once in the order incident → glass → transmitted; never move the copy or booth, loop the waves, or replay the sequence after a breakpoint change.

The 2026-08-24 structure-marker correction separates live target markers from the non-uniformly stretched full-screen leader SVG. Keep each ring-and-dot marker in its own fixed-square 16×16 SVG and move that marker with pixel `translate3d()` coordinates from the Three.js projection; the leader path may remain in the 640×600 non-uniform line coordinate system. Never place the circular endpoint geometry back inside that stretched line SVG, because it visibly turns the ring into an ellipse.

The 2026-08-24 acoustic-boundary clarification makes the assembled GLB itself the coordinate source for the acoustic chapter. Project all eight corners of the centered product bounds through the live camera, publish the resulting screen rectangle, and align the incident endpoint, front-opening mask, transmitted start, and two restrained bracketed boundary gates to it. The gates bridge the 7–10px visual gap to the shell and draw with the acoustic section's entry scroll progress; they must reverse deterministically, never loop, and never move the product or copy.

The 2026-08-27 Tuliko dark long-scroll optimization keeps the five existing chapters, assets, Japanese copy, authoritative GLB, and one global rail while reducing the story span to roughly 775lvh desktop / 690lvh mobile. The structure chapter is a scroll-deterministic 30% expansion, 50% fully exploded reading hold, and 20% reassembly; all parts retain their existing semantic expansion directions and projected labels. Adjacent product states may only overlap across the final 15% of the outgoing stage: copy exits first, the model completes its state, and the incoming copy waits for a recognisable model. Do not use full-screen transition masks, chapter-local progress UI, or repeated product entrances.

The 2026-08-28 line-expression refinement adopts the approved Tuliko concept image: acoustic energy is conveyed by fine continuous core traces, sparse dotted baselines, and one short moving dash per lane instead of broad blurred glow. Incoming red traces compress at the front boundary; glass and outgoing sage traces remain thinner and calmer. Keep the existing deterministic SVG flow and low-amplitude lane breathing, plus all visibility and reduced-motion safeguards.

The 2026-08-28 reference-image implementation anchors the desktop hero at a left-of-product copy / full incoming red-field / 75vw authoritative booth composition. It adds only three quiet Japanese product callouts—`吸音内板`, `固定ガラス`, and `換気システム`—with complete hairline leaders and restrained endpoint rings. These notes are hero-only, use the live product shell variables, and are hidden on phone widths; retain the one global progress rail and all responsive acoustic safeguards.

The 2026-08-28 loading-screen refinement removes the visual product tag and two explanatory copy lines. Place one monospace percentage at the right end of the acoustic progress track instead. It may show only the actual readiness states (`00%` while the render/fallback is not ready and `100%` when it is); do not invent interpolated or time-based percentages.

The 2026-08-28 structure-index refinement follows the supplied exploded-assembly reference: cluster the suspended-assembly labels in an upper-right technical index and cluster the separated shell/floor labels in a lower-left index. Do not restore the previous full-height left/right label columns. Fixed labels and elbows still pair with their existing semantic parts; only final leader segments and endpoint markers follow live GLB projections.

The 2026-08-28 structure-leader refinement fixes the Japanese labels in clear outer side columns. A leader must never travel through the GLB toward an internal point: terminate it at the label-facing edge of the relevant exterior component instead. Use the roof for roof/lighting, the left shell for its nested left-side parts, the base or carpet for lower parts, and the outer acoustic-panel edge for the right-side index. The fixed label columns must stay clear of the product and the far-right global rail.

The 2026-08-28 structure-clarity refinement supersedes the prior projected endpoint-ring treatment for the exploded chapter: remove visible circular markers, because they overlap the rendered panels at narrow desktop widths. Leaders end exactly at the relevant exterior silhouette; use a 1px high-contrast line and clear 11px side labels instead. Do not reintroduce endpoint rings unless the user explicitly requests them.

The 2026-08-31 camera refinement uses a direct, product-centred front view for the entire 3D narrative. Keep the camera x-position and look target aligned to the product's current x-position; do not restore the previous horizontal three-quarter bias. The GLB retains its `Math.PI * 0.5` front orientation, so the booth remains square to the viewer while its stage position changes.

The 2026-08-31 structure-camera refinement adds one scroll-deterministic camera phrase to the construction chapter: gently dolly in and lift while the assembly opens, hold for the full 50% exploded inspection period, then return before the next chapter. It must derive only from `structureProgress`, reverse exactly, use no clock/loop, preserve the direct front view, and resolve to zero for reduced motion and outside the structure chapter.

The 2026-08-31 narrow-desktop structure safeguard applies at 1200px and below (but above the mobile breakpoint): use a wider camera fit and a restrained construction dolly/lift so the exploded roof stays below the fixed header and the base stays inside the viewport. The lower-left leaders use ordered, label-height lanes clipped to their exterior source bounds; they must not converge on differing projected points or cross one another.

The 2026-08-31 no-crossing structure-guide refinement supersedes diagonal and elbowed leader routing. Use a single horizontal lane per fixed side label, with the label column outside the product silhouette (20% on the left, 83% on the right). Do not add diagonal, vertical, or return segments to the exploded-structure guide paths.

The 2026-08-31 component-correspondence refinement supersedes the fixed 20%/83% label-column rule where it leaves a leader disconnected from its named GLB part. Project the actual bounds of every authoritative component and place each Japanese label in the immediately adjacent clear gap on its label-facing side; draw only the short direct segment from that label to the projected exterior edge. This keeps every label visibly tied to its real part without endpoint rings, crossings, routes through the product, or text overlapping the model. Keep roof and lighting in the upper-right index, and leave the global rail unobstructed.

The 2026-08-31 desktop-fit correction extends the header-safe exploded-structure framing to wide desktop layouts. The front-facing booth must keep both separated roof and base fully inside the viewport during the full inspection hold; preserve the gentle scroll-driven dolly/lift phrase, but reduce its amplitude before allowing a cropped roof or base.

The 2026-09-01 structure-to-acoustic handoff supersedes the longer construction tail: use `250lvh` on desktop and `220lvh` at 900px and below, preserve the exploded inspection hold, then reveal the acoustic field from the live reassembled-product boundary during the final shared overlap. Do not leave an assembled product alone on an empty stage before the acoustic chapter.

The 2026-09-02 glass-reflection refinement removes the broad vertical RoomEnvironment bands from the SPD01 glazing. Keep physical glass highly transmissive with restrained environment and clearcoat response, then use only one narrow, feathered, scroll-linked diagonal highlight inside the verified glass mask. The reflection must never read as a grey curtain or obscure the desk, handle, interior, or sound traces.

The 2026-09-02 acoustic-lane alignment uses one screen-space lane system for incident, in-glass and transmitted waves. Keep the glass mask at the real opening for clipping and reflection, but size its nested wave track from the live incoming-wave rectangle. Never scale the complete glass-wave group vertically because that moves lane centres away from the outside baselines; attenuation may reduce each lane's amplitude, not its y position.

The 2026-08-31 hero-placement correction sets the desktop hero stage centre to 64.5% so the SPD01 front plane aligns with the user-approved left edge of the hero product frame. Keep this value in both the CSS fallback and the scroll-state `stageCenters` source; do not attempt to offset only the fallback CSS because the runtime state owns the active variable. The sound field and product callouts continue to follow that common centre, while the hero copy and global rail remain fixed.

The 2026-08-31 product-clarity correction removes decorative Three.js lighting effects: no bloom, ground glow, contact shadow, particle field, environment reflection, or bright self-emission. Retain only restrained even lighting and a fixed matte deep-grey-green isolation plane behind the GLB so the dark frame, door, and exploded panels remain legible against the black page. The backplate is material separation, not an animated halo; do not restore a spotlight or a glowing base.

The 2026-08-31 material-lighting refinement supersedes the blanket no-reflection/no-shadow rule. Restore only a low-detail neutral room reflection and a low-opacity, local contact shadow so the authoritative glass, metal, textile, and panel finishes read naturally. Keep bloom, particles, floor glow, spotlights, strong self-emission, and harsh contrast disabled; lighting must remain even, matte, and subordinate to the product materials and separation backplate.

The 2026-08-31 backdrop refinement makes the separation plane a compact neutral graphite inner liner, not a sage-green product surround. Keep it slightly smaller than the enclosure and behind the door area only; it must not create a coloured rectangle outside the real model silhouette or be mistaken for an extra product layer.

The 2026-08-31 material-detail pass keeps the restrained three-point light rig but reduces flattening ambient light. Use the authoritative material classes to differentiate translucent reflective glass, satin sage shell panels, low-gloss structural metal and hardware, soft off-white desk/light surfaces, and high-roughness textile/carpet. Any highlight must reveal a real material edge or surface; do not reintroduce bloom, theatrical glow, or arbitrary lighting accents.

The 2026-08-31 brand-story page uses the same quiet-industrial Tuliko system at `/about/`: a dark, low-radius editorial layout, the supplied wordmark, the verified SPD01 product image, Japanese-first copy, and a single `導入相談` conversion path. It must not claim Japanese manufacture, joint Japan-China ownership, or unverified certifications or performance values. Frame China as the smart-manufacturing and precision-making base; frame Japan as the enterprise procurement/operation context and domestic service commitment.

The 2026-08-31 brand-story page should remain copy-light: use short Japanese statements and the supplied office-scene imagery to carry the narrative, rather than adding explanatory paragraphs.

When evolving the Tuliko brand-story page, prefer the information rhythm of a Japanese B2B company overview—brand point of view, product range, making and local service, then consultation—without copying competitors' styling, wording, unverified statistics, company profile details, or certification claims.

For the Tuliko company-introduction page, the confirmed reading logic is decision-first: define the brand, explain the office-use value, show the product range, explain manufacturing and post-installation service, state the standards of trust, then give the consultation path. Keep that sequence explicit and copy-light.

The current trust step is an information-for-planning section, not a generic values block: it must focus on model-specific acoustic testing or confirmation conditions, material/specification information, and installation constraints. Do not publish a performance value, certification, or legal claim unless its model and conditions are verified for the page.

The company-introduction page must expose unconfirmed future content as visible Japanese bracketed placeholders (for media, product specifications, files, cases, and company details), so the user can complete it later. Preserve these as clearly editable slots rather than replacing them with fabricated information.

The company-introduction page may display the supplied SGS report only in the post-planning verification area. Transcribe its report number, issue date, ISO 23351-1:2020 method, sample dimensions, stated conditions, and stated result exactly; identify it as a report-sample result and state visibly that it does not automatically apply to every Tuliko model until the target model and conditions are confirmed.

The verification area may also present the two supplied ISO 23351-1:2020 reports as a compact numerical record and chronology: 2026-05-06 / 05-12 sample receipts, 2026-05-28 report issue, one sample per report, five recorded door operations, and 260m³ reverberation-room volume. Keep those as test-record events rather than company-growth claims, and do not map either report to a Tuliko model without confirmation.

The approved company-page presentation rhythm is evidence-first: after the hero, use a four-item factual metric strip; then state the work values, show the product and space imagery, explain the delivery path, and present dated verification records before the consultation. This is a presentation pattern only: do not replace missing Tuliko business facts with financing, patent, volume, partner, award, or growth metrics from other brands.

The company-introduction page now uses restrained decision-oriented interaction: selectable work scenes, an expandable selected-product panel, a scroll- and click-highlighted delivery sequence, and expandable planning-information cards. Maintain keyboard/button semantics and reduced-motion support; never trade verifiable procurement information for decorative motion.

For the company-introduction page, use Japanese decision labels rather than decorative English section names, and reserve gray media placeholders only for genuine narrative moments. Do not add a second hero-media placeholder when the approved SPD01 hero already establishes the product.

When explaining product advantages on the company-introduction page, prioritise the supplied office-scene imagery, product imagery, and user-controlled product video over longer explanatory copy. Company scale must be shown only with attributable factory, inspection, installation, or public service-location material; until those assets are supplied, retain a clearly labelled media slot rather than inventing counts, sites, or capacity claims.

All incomplete media slots on the company-introduction page must be visibly gray and include an in-page production specification. Use 16:9 with a minimum export of 1920 × 1080 px for hero/manufacturing video or wide visual slots, and 3:2 with a minimum export of 1800 × 1200 px for workplace and project photography slots.

The 2026-08-31 SPD01 product-reference refinement uses the supplied local `spd01-green-hero-cutout.webp` as the exact visual baseline: a satin grey-green exterior shell, near-black top/base/frame hardware, neutral grey felt interior, translucent neutral glass, soft-white desk, charcoal carpet, and a restrained warm ceiling strip. Preserve the authoritative GLB and its semantic geometry; material changes must map to those existing classes rather than replacing it with a generic coloured shell. The hero may hold the matching three-quarter presentation angle, but it must interpolate back to the exact front elevation before structural callouts become active; all non-hero narrative states retain the direct front view. Keep broad, low-contrast studio lighting, only a local soft contact shadow and neutral interior liner, and no bloom, stage glow, particles, or theatrical contrast.

The 2026-08-31 construction-motion refinement adds one reversible orientation phrase inside the structure chapter: after the assembled product is recognisable, it can travel right by about 12% of its width and turn toward the established grey-green three-quarter side by about 27 degrees while the parts begin opening. It must settle into the direct front elevation before the 50% exploded reading hold. The chapter headline exits before the technical leaders enter; leaders then fade before the reassembly begins. Each structure label stays near its own projected component edge with a horizontal leader only. Tiny vertical label offsets may separate labels that share a projected height, but do not add crossing, diagonal, or through-model leaders. The phrase must remain scroll-deterministic, reverse exactly, and resolve to the stable front/assembled state for reduced motion.

The 2026-08-31 chapter-background refinement keeps the homepage's existing `#0c0f0e` Three.js product-theatre background unchanged. From the construction chapter onward, interpolate the canvas background to only a slightly lighter deep grey-green (`#131816`) so product materials and technical labels remain legible without turning the long-scroll experience into a light theme. The change must be continuous around the hero-to-structure handoff and resolve deterministically on anchor refresh, reverse scroll, and reduced motion.

The 2026-08-31 exploded-orbit refinement extends the structure chapter after its front-facing annotation beat: while parts remain in the authoritative full-exploded state, rotate the whole product exactly 360 degrees over scroll progress 48–79% and move the desktop camera up to a true 45-degree top-down view from 52–78%. Restore the normal front camera during reassembly; compact screens may use a reduced but still legible top-down angle. Technical labels must fade before the orbit begins so leaders never cross the turning/back-facing model. The orbit, elevation, labels, and reassembly must be one deterministic scroll state, fully reversible, and disabled for reduced motion.

The 2026-08-31 Anime.js reference-composition refinement keeps Tuliko's dark visual system and the existing single global rail, but gives the unlabelled exploded-inspection beat a restrained diagonal editorial frame: use a small scroll-deterministic camera sweep, a modest screen roll, and a compensating fit-out while the 360-degree product turn and top view run. The sine envelope must resolve to an exact level front frame at both ends, so technical labels remain limited to the earlier readable front-facing beat and no chapter-local progress bar or copied light-theme composition is introduced.

The 2026-08-31 extended structure-inspection refinement increases only the physical scroll runway of the construction section (desktop 228lvh; mobile 188lvh) while preserving the approved 30% opening / 50% full-explosion / 20% reassembly proportions. In the front-facing exploded reading beat, technical labels must alternate left and right in brief overlapping cues rather than appear as one dense wall. Each cue still terminates at its authoritative component's outward silhouette using a short horizontal line, clears before the orbit begins, and may never cover, cross through, or obstruct the product.

The 2026-08-31 product-scale refinement treats the hero, structure, acoustic, modular, and interaction states as one camera-scale system. Preserve the approved generous hero and fully exploded structure fits (roof/base remain header-safe), but bring the assembled product closer in all later narrative chapters using smoothly interpolated per-chapter camera distances. On compact screens, apply the same assembled-state increase while compensating the explosion-fit multiplier so the fully separated booth remains wholly visible. Product boundaries, sound fields, labels, module panel, interaction panel, and global rail must retain their existing clearances; never enlarge by cropping or by moving fixed copy/rail UI.

The 2026-08-31 extended-inspection duration refinement increases the structure chapter to 252lvh on desktop and 208lvh on mobile. This adds roughly one fifth more physical scroll distance to the fixed 50% full-explosion observation phase without changing the 30/50/20 state proportions, the 360-degree inspection sequence, or the safe full-product fit. Keep the state scroll-deterministic—there is no time-based hold or looping playback—so rapid/reverse scroll, hashes, resize, hidden-page pause, and reduced motion remain coherent.

The 2026-08-31 front-elevation refinement supersedes the earlier optional hero three-quarter presentation: every normal assembled state, including the hero, must use the exact front elevation of the authoritative GLB. Keep the local three-quarter WebP only as a load-error fallback. The hero's waves and feature annotations now derive from the live front-model projection rather than that raster crop: incident traces stop before the left exterior edge, glass traces stay within the door opening, and outgoing traces start after the right exterior edge. The full 360-degree exploded inspection remains an explicitly separate later construction action and returns to front before reassembly.

The 2026-08-31 hero-framing refinement moves only the desktop hero's front-facing GLB about 50px right through a camera framing offset, placing it in the approved clear product field while leaving the left editorial copy and the global rail fixed. Fade that offset out during the hero-to-structure handoff; no other chapter inherits it. Hero waves and the three callouts continue to use the live projected GLB boundary, so they move together with the product and never overlap its exterior silhouette.

The 2026-08-31 exact-front product reference supersedes the earlier `Math.PI * 0.5` front-yaw value. The supplied `SPD01-灰绿-v2-正.png` defines the true viewer-facing relationship: the worktop is on the viewer's left, the door handle is on the right, the sage uprights remain symmetric, and the ceiling strip and dark carpet retain their material hierarchy. Use `-Math.PI * 0.5` as the authoritative GLB front turn in every assembled state; the later exploded inspection still adds exactly one reversible 360-degree turn and returns to this front. Keep the compact neutral inner liner visible behind the glass from the first frame so the dark door hardware stays legible on desktop and compact layouts without creating a coloured halo outside the shell. After this yaw correction, structure labels must follow the newly projected component sides: desk/acoustic-panel callouts use their clear left-side gaps, column/side-panel/fixed-glass callouts use their clear right-side gaps, and every horizontal leader remains in its own component-bounded lane so no two lines visually merge through the product.

The 2026-09-01 GhostFibers scope refinement makes the restrained grey-green OGL fiber field a continuous site atmosphere rather than a hero-only effect. Keep one shared low-power instance visible through every product-story chapter, the video and post-story sections, and the React brand-story page. Use low opacity, screen blending, and a soft edge mask so it never competes with product geometry, annotations, text, controls, or media. Pause it during loading and route departure; document visibility, offscreen state, and reduced motion must continue to stop the render loop or hold a static frame.

The 2026-09-01 acoustic simplification keeps the assembled WebGL booth and its central glass-wave mask visible. Remove only the two volume-meter strips and the dotted product-boundary gates from the acoustic chapter. Keep the chapter copy, incident red waves, transmitted sage waves, ambient fibers, and global progress rail. Other product chapters retain their existing WebGL presentation.

The 2026-09-01 auxiliary-panel simplification removes the `構成モジュール` card from the modular chapter and the `ばね復帰操作` draggable card from the usability chapter. Keep the product, chapter headline, concise summary, facts row, ambient fibers, and global progress rail. The usability copy must describe the visible door, desk, lighting, and entry relationship rather than promise a removed drag interaction.

The 2026-09-02 five-role review refinement makes direct chapter navigation land on each story section's established reading frame at about 16% of its pinned distance, while natural forward and reverse scrolling still includes the complete preparation frame. The one global progress rail belongs only to the five WebGL story chapters and must fade out before the product film, SKU selection, consultation, and footer; it may never move under the compact header or cover the mobile menu.

The same review makes customer-facing technical labels Japanese-first and readable: retain official model names such as `SPD01`, but use Japanese chapter registers and fact values, with technical support text at 11–12px rather than 7–10px. The video prelude must expose a visible qualitative audio description and direct paths to structure, SKU selection, and consultation. User-enabled audio may never auto-resume after leaving and re-entering the video viewport.

The acoustic chapter must state that its waves are a schematic explanation and link to the verified report area without copying a result onto an unconfirmed SKU. The consultation area shows only verified product/contact information, describes variable service conditions in customer-facing language, and keeps `mailto:` as an explicit mail-client action with a copyable inquiry fallback. Delight remains limited to restrained instrument-like tick confirmation and 2px directional CTA-arrow feedback; do not add particles, product bounce, mouse-follow, celebratory effects, or invented success states.

The 2026-09-02 assembled-chapter scale refinement makes the desktop acoustic, modular, and interaction chapters share the same 1.30 camera-fit value, so SPD01 retains one visual height across all three explanations. Acoustic and modular also share the established right-hand screen position, while interaction keeps its own horizontal composition. Preserve every chapter's copy runway, wave field, rail clearance, and reversible scroll state. Compact screens retain their safer farther modular and interaction fits so the product never clips against the right edge or bottom progress rail. Do not simulate parity by cropping the booth, scaling DOM overlays, or moving fixed navigation.

The 2026-09-03 product-model replacement uses the supplied `Snapod(小号)白色静音仓.glb` as the current geometry source. Its 252 separately exported CAD roots are reoriented, deterministically mapped into the ten established semantic assembly modules, repaired with explicit glass and hardware material roles, and losslessly Meshopt-compressed as `snapod-spd01-white-semantic.glb`. Keep the prior semantic STEP model only as the classification reference and rollback asset. Runtime presentation continues to use the approved SPD01 grey-green finish until a separate verified white-product color direction is requested.

The 2026-09-03 entry-flow refinement makes the full-viewport product video the first completed-load view for every fresh home-page document load, including legacy shared URLs ending in `#hero`. During boot only, normalize that legacy hash to the unadorned home URL and reset scroll restoration to the video prelude. In-page logo, TOP, `製品を見る`, and footer links continue to use `#hero`, which must return to the separate assembled-product overview without replaying the prelude. Autoplay may begin only after the loading screen completes, muted and from the start; never auto-resume audio.

The 2026-09-03 supplied-GLB information refinement makes the uploaded `Snapod(小号)白色静音仓.glb` the source of truth for customer-facing structure facts. Report its GLB bounding box as W1048 × D1000 × H2320 mm, its 252 exported CAD elements, and the ten display classifications; explicitly state that the dimensions are bounding-box values and exclude installation clearance. Use customer-readable assembly names (`天板ユニット`, `ベース`, `コーナーフレーム`, `外装パネル`, `ドアユニット`, `固定ガラス`, `内装パネル`, `デスク`, `フロアマット`, `天井照明`) rather than implying that the file contains only ten physical parts or restoring unsupported acoustic/material claims.
