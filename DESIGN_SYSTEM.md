# Tuliko 首页设计系统

## 1. 项目概述

- **品牌名称：** Tuliko
- **产品名称：** SPD01 静音ワークブース
- **网站类型：** 单页沉浸式 B2B 产品首页
- **表面类型：** 首页为 Landing / Marketing + Experiential WebGL；`/about/` 为 Redesign + Landing / Marketing，采用 Standard DOM / CSS 媒体叙事
- **平台：** 响应式 Web
- **目标用户：** 日本企业的办公室与设施采购人员、空间设计师、内装与搬迁项目负责人
- **主要目标：** 让访客先理解产品外观、结构、隔音逻辑和模块组合，再进入导入咨询。

## 2. 品牌方向

- **视觉风格：** 静谧工业、日式留白、精密仪器界面
- **情绪与语气：** 冷静、克制、可信；不使用赛博朋克、夸张能量感和营销口号堆叠
- **设计个性：** 技术理性为主，产品材质的温度为辅
- **核心概念：** 用一条贯穿全页的“声学测量轨道”连接产品、声波、结构标注与滚动进度，让所有状态像同一台精密仪器的连续读数。
- **对抗性审查：** 隐去 Tuliko 名称后，页面仍可由窄体静音仓、红色入射波、鼠尾草绿色衰减波、实时结构引导线和单一右侧进度轨道识别。手写 Tuliko 字标只作为品牌层的柔性对比元素，不扩散到标题或正文排版。首轮弱点是导航与卡片过于像通用科技模板；已删去中部导航、移除装饰性毛玻璃并统一为实色仪器面板。第二轮弱点是面板中混入装饰性英文标签和无来源比例数据；现已改为日文功能标题、等权分类条、真实序号和“数量比ではありません”说明。
- **公司介绍页对抗性审查：** 隐去品牌名后，页面仍可由“左侧七章决策索引＋右侧工程检视舞台＋结果与适用条件同屏＋单一信号红”的跨境质量校准台识别。叙事不靠国旗、地图或文化符号，也不把“中国”简化为生产、“日本”简化为质量；三方职责改为“中国：智能制造／精密加工、Tuliko：要求定义／规格整合、日本：安装条件／本地支持”，质量验证贯穿全流程。本轮已消除公开占位文案、误导性方法配图、过度留白与移动端证据后置，并为所有 CG 图统一增加非实绩标识。
- **参考方式：** 不复刻第三方品牌资产。首页继续以用户提供截图、本地产品图和本地 STEP 转换 GLB 为准；`/about/` 额外参考 [Serve Robotics Company](https://www.serverobotics.com/company/) 的“主张→数字证据→事件”节奏、[Manufact](https://studio.peakclaritypoint.com/projects/manufact) 的可见工业网格与大数字、[KOKUYO Space Solutions](https://www.kokuyo.com/en/about/expanding-domains/space-solutions/) 的编号叙事，以及 [Soundbox 会社概要](https://soundbox-pod.co.jp/about-us/) 的企业资料顺序。只迁移结构动作，不复制色彩、文案、数据或品牌资产。
- **文案规则：** 日文短句、事实优先、避免未经验证的 dB/效率数字和伪精确比例。故事章节不叠加 `NEXT`、下一章名称或预览缩略图；最终 CTA 使用 `導入相談`。不使用“革命性”“无缝”“行业领先”等空泛表达；仪器面板不混用无信息量英文装饰标签。

## 3. 色彩系统

首页实时值位于根目录 `tokens.css`；公司介绍页使用同一暖黑／纸白／红色语义，并在 `brand-story-v2.css` 内集中声明页面级 token，组件不得写入新的裸色值。

| 角色 | OKLCH | 近似 Hex | 用途 |
|---|---|---|---|
| 60% 主背景 | `oklch(25.2% 0.006 62)` | `#252423` | 页面、Canvas 底色 |
| 30% 深层背景 | `oklch(21.8% 0.006 62)` | `#1f1e1d` | 轨道、暗部、接触阴影 |
| 抬升表面 | `oklch(28.7% 0.006 62)` | `#2a2928` | 模块与交互面板 |
| 主文字 | `oklch(96.8% 0.006 68)` | `#f6f4f2` | 标题、主要操作 |
| 次文字 | `oklch(79.8% 0.009 68)` | `#c3bfba` | 正文、说明 |
| 弱文字 | `oklch(66.5% 0.009 68)` | `#97938e` | 技术标签、辅助信息 |
| 10% 信号色 | `oklch(66.5% 0.205 29)` | `#f14f45` | 入射音波、活动游标、局部状态 |
| 声学语义色 | `oklch(61.5% 0.055 153)` | `#718b76` | 透射/衰减音波、产品侧板语义 |
| 焦点色 | `oklch(78% 0.148 48)` | `#f39b69` | 键盘焦点环 |
| 公司介绍页墨黑 | — | `#0b0c0a` | `/about/` 主背景与固定章节索引 |
| 公司介绍页面板 | — | `#111210` / `#181917` | `/about/` 证据板、流程与图注 |
| 公司介绍页纸白 | — | `#f6f6f2` | `/about/` 主文字与主要操作 |
| 公司介绍页辅助灰 | — | `#787a75` | `/about/` 章节编号与非关键技术元信息；正文与操作标签使用更亮的 `#9b9d98` |
| 公司介绍页信号红 | — | `#ff3b30` | `/about/` 当前章节、检证结果与焦点环 |

**模块数据色：** 框架红、外装橙、玻璃青、吸音内装青柠、家具与电装紫。每一色必须与名称同时出现，不能只用颜色表达。

**对比度（WCAG 2.1）：** 主文字/背景 `14.50:1`；次文字/背景 `8.45:1`；弱文字/背景 `5.21:1`；信号色/背景 `4.75:1`；焦点环/背景 `7.49:1`。数值由当前 OKLCH token 转为 sRGB 后计算。

**使用规则：**

- 大面积只使用暖碳黑与相邻表面色。
- 红色面积不超过单个视口约 5%，不用于大段文字或大面积背景。
- 普通卡片依靠色阶与阴影分层；细线仅保留给测量轨道、结构引导、面板刻度和焦点等有功能的技术元素。
- 页面只提供深色模式；后续若增加浅色模式，必须以语义 token 重映射并重新计算对比度。

## 4. 字体系统

- **品牌个性：** 工业技术 + 日文可读性
- **字体组合：** 标题与正文使用本地 `DINish`，日文回退 `Yu Gothic / Hiragino Sans / Meiryo`；技术数据使用本地 `Berkeley Mono`，最多三套字族。
- **选择原因：** DINish 提供窄体工业标识感，日文字体回退保证假名与汉字可读，Berkeley Mono 只承担测量、编号与部件标签。
- **Display：** `clamp(3.25rem, 5.2vw, 5.125rem)`，行高 `0.9–0.96`
- **H2：** `clamp(2.75rem, 4.45vw, 4.375rem)`；手机下限 `2.125rem`
- **Body large：** `clamp(1rem, 1.2vw, 1.1875rem)`
- **Body：** `clamp(1rem, 1.05vw, 1.125rem)`，绝不低于 16px
- **Caption：** 11px；仅用于技术标签，不能承担关键说明
- **Button：** 13px、粗体、单行不换行
- **正文行高：** 1.5–1.62；日文段落以约 22–34 个全角字符为目标宽度
- **响应规则：** 使用 `clamp()` 与小屏独立下限；标题允许语义断行，不裁切、不省略。
- **公司介绍页：** Berkeley Mono 负责拉丁数字、章节编号和测试信息，日文由 `Noto Sans Mono CJK JP` 回退。桌面导航 12px、活动章节摘要 16px、主导语 16–17px；9–11px 只用于报告编号、图注和流程元信息，不承担正文。日文标题字距控制在 `-0.04em` 左右；390px 首屏标题使用 38–43px 且最多三行，禁止 1–2 字孤行。

## 5. 布局系统

- **页面结构：** N9 边缘对齐导航 → H6 全屏产品折页首屏 → 结构分解 → 隔音比较 → 模块构成 → 实体拖拽体验 → 产品实景视频 → 独立 SKU 选择与完整产品展示 → 导入咨询与企业信息 → Ft2 单行收尾
- **宏观结构：** Narrative Workflow；每一章接续上一章的产品状态，不硬切场景。
- **容器：** 内容边距 `clamp(24px, 5vw, 92px)`；桌面最右侧固定预留 72px 进度轨道。
- **首屏构图：** 全屏产品视觉作底，文案落在左侧负空间，产品以约 `75%` 视口宽度为中心；四条完整红色入射轨迹在其左侧展开并在前框前收束，三条日文产品标注以完整细线连至端点环，同时为最右侧全局进度轨道留出安全区。不是卡片式 50/50 分栏。首屏在 700–800px 高度内完整显示标题、说明和产品焦点；不显示续览覆盖层，也不再并排放置两个大按钮。
- **首屏产品说明：** 桌面首屏在产品两侧保留三处细线引导：左侧吸音内板和换气系统、右侧固定玻璃。引导线以实时产品边界为基准，端点以小型空心圆收束；只在首屏出现，手机尺寸隐藏。
- **首屏声场边界：** 以权威透明产品图的实时渲染矩形和可见轮廓比例计算边界。红色入射波停在左侧框架外 7–10px，玻璃内波形只占前玻璃开口，鼠尾草绿透射波从右侧板外 7–10px 开始；不得用固定视口百分比把线画进侧板。
- **章节：** 每章使用 `100lvh` sticky 舞台，外层长滚动控制连续状态。五章故事总长为桌面约 `775lvh`、手机约 `690lvh`；桌面依次为 `145 / 220 / 145 / 140 / 125lvh`，手机为 `130 / 180 / 130 / 130 / 120lvh`。结构章的展开 / 完整停留 / 归位严格为 `30% / 50% / 20%`。
- **构造至遮音衔接：** 当前构造章滚动高度为桌面 `250lvh`、≤900px `220lvh`。爆炸观察段继续保留，末段归位时依据产品实时屏幕边界同步淡入遮音波场，禁止在两章之间留下只有完整产品、没有信息的空白停留。
- **玻璃反光：** WebGL 玻璃保持高透光与低环境反射，避免室内环境贴图形成大面积竖向灰带；前景只允许一条位于真实玻璃遮罩内、随滚动移动的窄斜向柔光，并保持桌板、门把、内饰与声波清晰可辨。
- **声波轨道对齐：** 左侧入射、玻璃内衰减、右侧透射统一使用相同的五条屏幕坐标基线。玻璃遮罩仍覆盖真实开口，但内部 SVG 轨道必须根据实时入射波矩形定位；只缩小波幅，不允许整体压缩玻璃波纹组而改变各条波纹的纵向位置。
- **CTA 流：** `導入相談` 是最终转化目标，页头可直接锚定咨询表单；操作章先进入产品映像，再进入 SKU 展示区，用户选定产品后进入咨询。故事章节不设置可点击或纯展示的“下一章”覆盖层；明确操作只保留在 SKU 选择和咨询提交等任务型区域。当前表单通过 `mailto:` 唤起访客邮件应用，并明确说明页面不保存数据；未接入后端前不得显示“发送成功”。
- **全局进度：** 页面只能存在一条进度轨道。桌面在最右侧垂直显示，≤900px 在故事章节中使用底部横向。轨道只覆盖 Canvas 叙事的 `TOP / 構造 / 遮音 / 構成 / 操作` 五章，并连续映射该叙事段滚动状态；映像、SKU、咨询和 Footer 不追加为全局刻度，按自然文档流继续浏览。手机端保留五个短标签并等分轨道，不退化为无标签刻度。
- **断点：** 320–699px 紧凑移动端；700–900px 平板；901–1100px 紧凑桌面；1101px+ 完整桌面。
- **移动端：** 首屏使用上下布局并完整缩小产品；结构技术标注折叠为图例。模块章和操作章都使用左下紧凑状态卡，并将 WebGL 产品避让到右侧。两类面板都位于底部进度条上方，不得覆盖产品。
- **公司介绍页布局变量：** `--story-copy-rail: clamp(300px, 28vw, 372px)`、`--story-stage-gap: clamp(12px, 1.5vw, 22px)`、`--story-stage-edge: clamp(16px, 2vw, 30px)`、`--story-stage-pad: clamp(20px, 2vw, 28px)`。桌面形成约 27/73 的“证据索引栏＋媒体检视舞台”，压缩两栏之间的无效黑色空隙。
- **公司介绍页首屏构图：** 不是普通 50/50 文图卡片；产品以右侧整块工程图纸舞台呈现并保留上下安全边界，左侧固定章节栏承担品牌主张与决策顺序。职责桥梁统一为“中国：製造・加工 → Tuliko：要求・仕様 → 日本：導入・支援”，并在首屏直接给出“日本企業向け静音ワークブース”和 `SGS 30.3 dB*` 证据入口；星号必须紧邻“仅限报告试料与条件”的说明。
- **公司介绍页整页滚动：** ≥981px 使用原生 `scroll-snap`，每章以扣除 64px 页头后的视口为舞台；不拦截滚轮。章节内部内容超过可用高度时，以 `scrollHeight > clientHeight` 自动切换为自然高度与 proximity snap，避免裁切。≤980px 取消强制整页吸附，并提供 44px 高的横向七章导航。

## 6. 组件系统

- **基础：** React 语义元素 + 原生 CSS；不引入第二套 UI 框架。
- **图标：** 只使用 `@phosphor-icons/react`。
- **按钮：** 页头 `導入相談` 保持纸白实色、深色文字；故事章节内不使用下一章按钮。SKU、表单等真实任务控件的触摸目标不低于 44px。
- **章节过渡：** 每章末段 `15%` 以共享滚动权重预置下一产品状态，先使模型可辨识、再显示文案；离场时先收文案、再完成模型状态。禁止额外生成 DOM 预告层、`NEXT` 标签、下一章名称、预览缩略图、全屏遮罩或滚动命令。反向滚动按同一进度恢复；reduced motion 下取消提前三维运动。
- **导航：** 左侧使用用户提供的 Tuliko 手写字标；桌面右侧依次显示 `Tulikoについて / 製品・事業 / 配置検討例 / 更新情報 / お問い合わせ` 与主按钮 `導入相談`。宽度低于 1100px 时五个栏目收进原生 `details` 菜单，CTA 保持可见；章节导航仍由全局进度轨道承担。
- **加载页：** 全屏暖碳黑仪器界面，使用 `Tuliko / SPD01` 产品语义与“高振幅声波逐级衰减为静线”的横向轨道，不使用通用旋转图标、虚构百分比或无来源指标。仅在首次挂载时显示，三维模型成功或回退渲染就绪后退出。
- **模块面板：** 左下角紧凑实色状态卡，桌面高度约 140px；保留顶部红色校准线、五段等权分类条和单行五组编号日文图例，并明确“数量比ではありません”。色段表达类别，不表达未经验证的数量或占比。
- **交互面板：** 与模块卡共用左下锚点和紧凑宽度；保留实色抬升表面、日文功能标题、外部音/静音中心双标签、真实可拖动 48px 状态点和关联状态文本。桌面须避开上方 CTA 与底部 Footer，移动端须位于全局进度条上方。
- **结构标注：** 固定文案与固定折点，只移动投影目标点和末段引导线；目标点使用环 + 点双重标记。桌面将照明、天板、支柱和固定玻璃收束为右上技术索引；侧板、前扉、吸音内板、桌板、地毯和底座收束为左下拆解索引，避免两侧形成无效文字柱。引导线保留非等比全屏 SVG 坐标系，目标点必须使用独立的 `16×16` 方形 SVG 和像素 transform 跟随投影，禁止把圆形端点直接放进会拉伸的全屏 SVG。
- **产品视频：** 采用本地 30.05 秒 SPD01 办公场景展示视频，使用单一 `100svh` 全屏舞台与 `object-fit: contain` 完整显示，不裁切产品。保留原生 controls、`preload="metadata"`、静态 poster，不自动播放；删除分镜缩略图、分幕跳转、左上标题遮罩、底部型号/状态信息栏和随时间切换的长文案。离开视口或页面隐藏时暂停；不伪造字幕或性能结论。
- **SKU 独立展示区：** 位于产品视频与咨询区之间，使用本地目录中可确认的 `SPD01 / SPD02 / SPD03 / SPD04 / SPD07 / SPD08 / SPD09 / SPD12 / SPD14` 九个真实 SKU。桌面区高度控制在扣除页头后的单一视口内：左侧集中显示短标题、当前型号与咨询动作；右侧把完整产品主图和 `3×3` 九格目录墙并列，九个型号必须同时可见，不使用横向滚动或分页。该 SKU 的色板紧贴主图下方，使用主图列的完整宽度显示颜色名；最多十色固定为五列两行，不允许因紧凑布局省略标签。型号格仅显示真实缩略图、编号、SKU 与配置类型，选中状态用顶部红色信号线表达。颜色使用文件名里的灰绿、土褐、米红、黑、白、卡其、阴影灰、光面灰以及 ARS / GXS 编码，且只显示该 SKU 实际存在的图像变体。目录里孤立的 `GSX / GXSS` 与完整 `GXS` 图组重复，按完整图组归为 `GXS`，不生成重复选项。移动端改为上下布局，但型号目录仍保持 `3×3`，避免退化为过长列表。完整产品图必须在四周保留安全留白并使用 `object-fit: contain`，顶板、底座和支脚都必须可见。默认选中 SPD01 灰绿图，不显示未经确认的价格、尺寸、容量或性能。
- **咨询表单：** SKU、参考颜色、公司、姓名、邮箱、咨询类型、详情七项；表单顶部仅保留当前 SKU、参考颜色与“変更”入口，完整选择界面不在表单内重复出现。桌面咨询布局必须在扣除页头后的单一视口内完整显示：公司/姓名为第一组双栏，邮箱/咨询类型为第二组双栏，详情使用紧凑三行输入区，输入控件高度不得低于 44px；左侧标题缩为两行并压缩企业资料行高。Footer 从下一屏开始，不占用表单视口。必填状态同时使用文字与信号点；移动端恢复单栏自然滚动，提交按钮明确为“メールで相談する”。
- **企业信息：** 品牌展示为 Tuliko，产品型号保留 SPD01；产品领域和咨询邮箱沿用项目内已有信息。法人名、所在地、电话未获正式资料前不编造，并在界面标注待正式资料确认。
- **Footer：** 位于咨询区底部，使用三组可核实信息：品牌与咨询邮箱、产品/章节导航、导入与企业信息导航；末行只保留 `© Tuliko`、品牌主张和返回顶部。不得照搬参考站的电话、地址、社交账号或产品分类。平板改为两列、手机改为单列，底部继续为全局进度轨道预留安全区。

## 7. 面板与章节样式

- **风格：** 低圆角、实色、单层抬升；不使用嵌套卡片和装饰性毛玻璃。
- **圆角：** 4 / 8 / 12px 三档；交互圆点例外为圆形。
- **阴影：** 仅浮层面板使用 `--shadow-panel`；产品接触阴影由 Three.js 场景承担。
- **渐变：** 只用于声波衰减、玻璃反射和页面暗部，颜色均来自红/鼠尾草绿体系。
- **玻璃效果：** 产品玻璃由 Three.js 材质和局部遮罩表达；UI 面板不使用玻璃拟态。

## 8. 图标系统

- **图标库：** Phosphor Icons
- **尺寸：** 按钮 16–17px，尾部外链 14–16px
- **颜色：** 继承文字色
- **规则：** 图标只辅助现有文字标签；禁止 emoji、禁止只靠图标表达关键动作。

## 9. 图片与资产规则

- **品牌字标：** `public/assets/brand/tuliko-logo.png`，基于用户提供的手写字标提取为透明底 PNG，仅出现在页头、加载页与页脚；不带矩形底板，也不作为正文装饰。
- **首屏产品：** `public/assets/products/spd01-green-transparent.webp` / 当前透明裁切资产，保持用户提供产品外观。
- **咨询 SKU：** `public/assets/products/catalog/` 内共有 50 张网页用 WebP，来自用户提供的 `11、SNAPOD 单体图`、`12、SNAPOD 单体图`、`13、SNAPOD 单体图` 三个目录。每个“SKU × 文件名颜色”优先采用 `v2-¾`，缺失时采用普通 `¾`；SPD14 采用每组编号最小的代表图。源 PNG 不直接进入网页，WebP 最长边限制为 1600px。型号配置文案只翻译文件夹已有名称，不推断尺寸、人数上限或官方色号。
- **三维结构：** `public/assets/models/snapod-spd01-authoritative.glb`，来源于本地权威 STEP 的优化转换。
- **产品视频：** `public/assets/video/spd01-structure-locked-v5.mp4`，H.264、854×480、24fps、30.05 秒，包含 AAC 立体声音轨；poster 为 `public/assets/video/spd01-structure-v5-poster.webp`。两者均来自用户提供的本地 SPD01 场景展示素材。原有四张时间点缩略图仅保留为未使用的归档资产，不进入当前页面结构。
- **格式：** 浏览器只加载 WebP 与 GLB，不直接加载 STEP。
- **处理：** 不程序化发明桌板、座椅、吸音毡、灯带或玻璃框架；摄影层与 GLB 之间采用低重叠暗切换。
- **AI 图片：** 除将用户提供的 Tuliko 字标提取为透明底资产外，产品与空间内容不使用 AI 新生成图片。
- **可访问性：** Canvas 提供文本替代；装饰 SVG 标记为 `aria-hidden`。

## 10. 动画与交互系统

- **库：** Anime.js 4.5.0 + Three.js + OGL 1.0.11。
- **Anime.js API：** 仅使用 v4 命名导入；不使用全局 `anime()`。
- **页面初次进入：** 先显示最短约 0.84 秒的声学加载轨道；本地 GLB 成功或 WebP 回退状态可用后，轨道收束为静线并退场，再触发产品揭示，避免加载遮罩吞掉首屏动作。产品位置保持固定，不从视口外滑入。首次进入使用一次性三段编排：约 1.2–1.5 秒低亮度显形、窄幅红/鼠尾草扫描线掠过产品、Three.js 冷白主光分两段提升；随后才依次展开外部、玻璃内和透射声波。动作不循环、不因滚动或 resize 重播，刷新到非首屏锚点时直接显示稳定终态；加载异常约 7 秒后自动进入可用回退，不能永久阻塞页面。
- **About 产品播放器：** 首屏从用户提供的 `11、12、13、SNAPOD 单体图` 中筛选 9 个已确认 SKU，以响应式 WebP 轮播展示。每款同时提供 1600×2026 常规资源和 2368×2999 高画质资源；浏览器按展示宽度与设备像素比选择，并提前解码下一款。两组资产统一清除透明通道内的大面积白色地台光，只保留底座接触阴影。信息按“型号 / 尺寸级别 / 内部配置 / 颜色”呈现，不推断毫米数。每款保持 4.6 秒阅读停留，桌面支持上一款、停止／再生、下一款和直接选择型号；型号选择使用 roving `tabindex` 与方向键。移动端隐藏拥挤的九项直选条，仅保留三个不低于 44px 的播放控制。产品切换只做 280–320ms 双层交叉淡化和短规格更新，不缩放、不位移、不使用扫光。页面隐藏、键盘聚焦或 reduced motion 时暂停；reduced motion 下直接显示稳定终态。
- **声场揭示：** 首次显示按“红色入射波到达外壳边界 → 玻璃内细线衰减 → 右侧透射波展开”依次发生；只使用水平 `scaleX` 与一次性透明度/刻度变化，不移动产品、不循环、不因断点切换重播。
- **持续波动与流向：** 4 条入射线、5 条玻璃线和 5 条透射线使用无辉光的细核心线、稀疏点状基线及一个短移动高光表达方向；各完整 SVG 声道组循环 `scaleY`，振幅依次收敛为约 `0.92–1.075`、`0.965–1.04`、`0.98–1.03`。每条路径保留归一化 `pathLength=100` 的短虚线高光 `stroke-dashoffset` 动态。不得移动玻璃圆点、产品或文案，也不得通过随机 morph 改变声学关系。页面隐藏时暂停，reduced motion 下不创建循环。
- **音量分段条：** 不再使用每段垂直弹起或整组硬淡入。可见度由 Hero 与遮音章节的实时滚动位置统一计算，红色入射条和鼠尾草绿透射条均沿声流方向逐段 `scaleX` 展开，离开章节时按相反顺序收束；文案只做 3px 内的低幅位移。状态必须可反向、可快速跳转且刷新到 `#acoustic` 时直接匹配当前滚动位置。reduced motion 下改为章节级瞬时稳定状态。
- **遮音章节边界：** 由 Three.js 将组装后 GLB 的八个包围盒角点实时投影为屏幕边界。红色入射端和鼠尾草绿透射端分别对齐左右外壳，并使用带短横帽的虚线“声学门”跨过 7–10px 留白连接产品；玻璃细线按同一边界裁切到正面开口。声学门随章节进入滚动进度绘制，可反向回放，不循环。
- **章节切换：** 同一滚动权重同步 Canvas、产品混合、文案、面板和全局进度；正文只做功能性淡入，不重复弹跳。
- **全站纤维环境层：** GhostFibers 在产品故事的全部章节、视频区块、后续产品区块和 `/about/` 持续显示。使用两层灰绿纤维、30fps 与 0.65 DPR，并通过低透明度、`screen` 混合和边缘遮罩保持在产品、文案与媒体之后的感知层级。加载和离场时暂停；页面隐藏、离屏与 reduced motion 由组件自身停止循环并保留静态首帧。
- **Hover：** 仅一个信号；按钮最多上移 1.5px 或变色，禁止缩放与阴影叠加。
- **Pressed：** 100ms 内下压 1px；焦点环立即出现，不动画。
- **拖拽：** `createDraggable` 提供真实拖拽，`createSpring` 只用于释放回中。
- **Reduced motion：** 关闭空间位移、灯光扫动与滚动绘制；保留立即可读的最终状态，滚动行为改为 `auto`。
- **公司介绍页互动：** “性能／仕上げ／運用”使用完整 `tablist / tab / tabpanel` 语义，并支持左右方向键；型号选择使用 roving `tabindex`。章节点击会立即更新当前位置再滚动，移动章节条自动把当前章带入可视区域。`prefers-reduced-motion` 下停止轮播、取消折叠过渡并即时跳转；所有按钮、链接和章节导航具备 `:focus-visible`、hover 与 pressed 状态。证据区提供可下载／打开的原始 PDF，不能只显示扫描图。
- **页面隐藏：** 暂停 Anime.js 与 Three.js；恢复时不重播一次性动画。
- **性能预算：** 桌面目标稳定 60fps，移动端自动降低 DPR、粒子/刻度和后期效果；动画只改变 transform、opacity、SVG draw 或 Three.js 对象属性。目标 LCP <2.5s、CLS <0.1、INP <200ms，除非有真实测量不得声称已达到。

## 11. 可访问性

- 主文本和辅助文本均通过 4.5:1；焦点环通过 3:1。
- 所有交互元素具有 `:focus-visible`、按下态和不小于 44/48px 的触摸目标。
- 页面提供“本文へ移動”跳转链接；使用一个 `h1` 和顺序 `h2`。
- 进度状态同时使用位置、长度和文字，不只使用颜色。
- 支持 `prefers-reduced-motion`、`prefers-reduced-transparency`、200% 缩放和安全区。

## 12. 核心栏目与 SEO

- **六个核心 URL：** `/`、`/about/`、`/business/`、`/cases/`、`/news/`、`/contact/`。所有栏目使用真实路径，不使用 Hash 充当独立页面。
- **元信息：** 六页分别配置独立 `title`、`meta description`、Open Graph 标题与描述，并允许 `index,follow`；正式域名确认后再生成 canonical 与绝对 URL sitemap，禁止用本地域名占位。
- **咨询覆盖：** 六页均提供前往 `/#consultation` 或已确认邮箱的入口；未确认电话与即时聊天服务前不得显示虚构号码或在线状态。
- **图片替代文本：** 内容图片说明产品型号、颜色或空间情境；加载页品牌图和 WebGL 参考层等纯装饰图使用空 `alt` 并标记 `aria-hidden`。
- **配置检讨例：** `/cases/` 当前使用本地空间图与产品图提供两个配置检讨示例，每例三张图；必须明确标注“不是实际纳入案例”。真实案例指标需待项目种类、地区、规模、服务内容、至少三张完成图与发布许可齐全后再计入。
- **更新信息：** `/news/` 只记录实际完成的网站或产品资料更新。当前月已有两条记录，但“月均两篇”属于持续运营指标，不能用单月数据宣称长期达标。
- **验收：** 构建后自动检查六页存在、标题与描述唯一、允许抓取、内容图 alt 完整、咨询入口覆盖；浏览器另测 360 / 768 / 1440 宽度无横向溢出。

## 13. 项目特定的反模板规则

- 禁止恢复彩虹圆环、平面科技光圈或漂浮产品。
- 禁止为每章再造独立进度条。
- 禁止虚构 dB、降噪百分比、客户数量或性能排名。
- 禁止把视觉构成权重包装成产品数量或比例；没有来源时使用等权分类，并明确标注为概念图。
- 禁止在日文仪器面板中加入 `MODULE / GROUPS / CONTROL` 等无信息量装饰英文。
- 禁止正文逐字、逐词、逐章反复跳动；首次展示之外保持稳定。
- 禁止覆盖或重新发明本地权威产品几何。
- **项目例外：** 技术引导线、进度轨道和声学基线可使用 1px 规则线；这是信息编码，不是装饰性卡片边框。日文段落按全角字符测量，不强制套用英文 45–75ch 宽度。

## 14. 后续页面要求

未来页面必须复用本文件中的色彩、字体、间距、组件、图标、面板、动画和产品资产规则。除非用户明确更新本文件，不得新增另一套视觉语言。

- **品牌故事页：** `/about/` 使用同一暖碳黑、细线、DINish / Berkeley Mono、低圆角和 Tuliko 手写字标，但采用静态编辑叙事而非 WebGL 滚动舞台。内容按“中国的智能制造与精密加工→Tuliko 的要求定义、规格整合与验证→日本的安装条件与本地支持”展开；质量验证贯穿三段，不归属于某一国家。页面中的“日本企业的质量要求”专指采购、安装、运用和维护的确认事项，不等同于日本制造、法定认证或中日合资，也不得补写未经验证的认证、保修或服务承诺。

## 15. 更新策略

色彩、字体、组件、动画、面板、图标、图片或 3D 资产规则变化时，必须同步更新本文件，避免设计系统与线上界面漂移。

- **遮音章节减法规则：** 遮音章节保留中间 SPD01 WebGL 模型与玻璃波形遮罩，仅移除左右音量条和产品边界辅助线；章节文案、红色入射波、灰绿色透过波、全站纤维环境层与全局进度轨继续显示。其他章节继续显示产品模型。
- **模块与使用章节减法规则：** 模块章节不显示“構成モジュール”卡片，使用章节不显示“ばね復帰操作”拖拽卡片。两章均保留产品、章节标题、摘要、三项事实信息、纤维背景与全局进度轨；使用章节文案改为门、桌面、照明与入室动线的可见关系，不再描述已删除的拖拽交互。
- **五角色验收规则（2026-09-02）：** 直接进入 `#structure / #acoustic / #modular / #interaction` 时定位至约 16% 的稳定阅读帧，保证标题、摘要与事实行可见；自然滚动仍保留完整准备段。全局进度轨只覆盖五个 WebGL 故事章节，进入产品影片、SKU、咨询与 Footer 后隐藏，移动端不得覆盖展开菜单。
- **信息与可访问性：** 面向日本企业用户的章节序号、事实值与规格说明使用日文，正式型号 `SPD01` 等除外；技术辅助文字采用 11–12px 下限。视频首屏提供可见的定性声音转述与结构、SKU、咨询捷径；用户开启声音后，离开并返回视口不会自动恢复有声播放。
- **证据与咨询：** 遮音波纹明确标注为模式表达，并链接到 `/about/#about-evidence` 的试验条件和报告原本，不把未确认适用性的数值复制到首页。咨询表单将 `mailto:` 明确表述为打开邮件客户端，同时提供“相談内容をコピー”回退；不得提前宣称已发送或已打开成功。
- **克制的趣味反馈：** 仅允许进度刻度一次性 220ms 确认和 CTA 箭头 2px 方向位移；reduced motion 下静态显示。禁止产品弹跳、鼠标跟随、庆祝动画、额外粒子、虚构完成状态或新的循环光效。
- **遮音／模块／操作产品尺度一致性（2026-09-02）：** 桌面端三个组装态章节统一使用 `1.30` 相机取景系数，产品在遮音、模块和操作页保持同一可见高度。遮音与模块继续共享右侧视觉中心，操作章保留自身水平位置；章节切换不得通过临时缩小产品制造留白。紧凑屏幕继续使用原有章节安全取景，避免模块或操作章产品贴边、遮挡文案或被底部进度轨裁切。

### 公司介绍页

- `/about/` 的栏目编号使用日文信息标签，保持采购决策路径可扫描，避免仅作装饰的英文栏目名。
- `/about/` 的空白背景使用 48px 工业校准网格、左栏边缘刻度和对应的真实章节数字水印；移动端降为 32px 网格并降低水印对比度。背景只使用既有暖黑、纸白透明线与信号红，不引入氛围光、虚构参数或装饰性英文状态。
- 灰色媒体位只服务于“利用场景、制造与服务、导入事例”三处叙事节点；首屏已展示权威 SPD01 产品资产时，不重复放置主视觉占位。
- 产品优势优先通过办公场景、产品细节和用户可控的视频来表达；制造规模仅在提供可归属的工厂、检品、安装或服务网点素材后展示，不能用推测数值补足。
- 试验报告属于采购验证信息：置于“导入前确认”之后、咨询之前，原始报告图与结构化条件/结果并列；任何结果都必须紧邻适用范围说明，不能泛化到未确认型号。
- 已确认的测试数值可采用“四格数字带＋三步时间轴”表达；每个数字与日期必须可追溯至报告，时间轴描述试验记录而非企业发展规模。
- 公司介绍页采用“品牌定位→成立原因→再设计方法→可核验证据→交付体制→风险与适用范围→分层咨询”的七章顺序。首屏用三段职责桥梁说明因果关系；报告数字只证明对应试料和试验条件。公开页面不得出现方括号占位或把未确认内容写成承诺；未知的服务、保修和维护范围统一表述为“条件确认后个别说明”。CG 必须标注“配置イメージ（CG）／導入事例ではありません”，真实工厂、日本本地团队与导入案例素材到位后方可替换为规模或实绩表达。
